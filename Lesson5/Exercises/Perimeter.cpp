unsigned long perimeter(unsigned short length, unsigned short width);

int main()
{
    return 0;
}

unsigned long perimeter(unsigned short length, unsigned short width)
{
    return ( 2 * length ) + ( 2 * width );
}